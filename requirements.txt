# LangChain packages (Python 3.13 compatible)
# Note: AgentExecutor and create_openai_functions_agent were removed in LangChain 1.0.0
# Pinning to 0.0.350 (version with AgentExecutor in langchain.agents)
# IMPORTANT: Use OpenAIFunctionsAgent.from_llm_and_tools() instead of create_openai_functions_agent()
langchain==0.0.350  # Main LangChain package with AgentExecutor support
langchain-core==0.1.23  # Core LangChain functionality (compatible with 0.0.350)
langchain-openai>=0.0.5  # OpenAI integration (compatible with 0.0.350)
# Note: langchain-huggingface is not compatible with langchain 0.0.350
# Using HuggingFaceEmbeddings from langchain.embeddings.huggingface instead
langchain-community==0.0.10  # Community integrations (compatible with 0.0.350)

# Vector store and embeddings (Python 3.13 compatible)
faiss-cpu>=1.8.0  # Updated for Python 3.13 support
sentence-transformers>=2.7.0  # Updated for Python 3.13 support

# Document loaders dependencies
pypdf>=4.0.0  # Updated version with Python 3.13 support

# Langfuse for tracing and evaluation
langfuse>=2.50.0  # Updated for Python 3.13 support

# Environment variables
python-dotenv>=1.0.1

# Core dependencies
# Note: langchain 0.0.350 requires numpy<2, so we use numpy 1.x for compatibility
numpy>=1.24.0,<2.0.0  # NumPy 1.x for compatibility with langchain 0.0.350

